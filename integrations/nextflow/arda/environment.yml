channels:
  - conda-forge
  - bioconda
dependencies:
  - python >=3.10
  - bioconda::mmseqs2 >=15
  - pip
  - pip:
      # seqtree (needed by `arda rnaseq correct`) is a core dependency since 2.5.5 -- no extra.
      - arda-mapper==2.5.6
